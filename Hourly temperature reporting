# Write a loop to print all elements in hourly_temperature. 
# Separate elements with a -> surrounded by spaces. 
# Sample output for the given program: 90 -> 92 -> 94 -> 95 
# Note: 95 is followed by a space, then a newline.

hourly_temperature = [90, 92, 94, 95]

for elements in range(len(hourly_temperature)): # for __ in __ with range(len(hourly_temperature)), your elements var will be the index. 
    value = hourly_temperature[elements] # Then hourly_temperature[elements] gets the value.
    if elements >= len(hourly_temperature) - 1: # Remember to use length minus one in the if.
        print(value, end = ' \n')
    else:
        print(value, end = ' -> ')
